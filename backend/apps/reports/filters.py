# This app has no django-filter FilterSet classes.
# Report endpoints accept plain query parameters (estado, plan_id, mes, etc.)
# and validate them inline in the view. See docs/architecture.md for rationale.

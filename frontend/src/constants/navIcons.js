import {
  LayoutDashboard,
  Users,
  CreditCard,
  MonitorPlay,
  LineChart,
  Settings,
  ScanLine,
  Activity,
} from 'lucide-react'

export const NAV_ICON_MAP = {
  chart:        LayoutDashboard,
  people:       Users,
  card:         CreditCard,
  monitor:      MonitorPlay,
  'chart-line': LineChart,
  gear:         Settings,
  scan:         ScanLine,
  activity:     Activity,
}
